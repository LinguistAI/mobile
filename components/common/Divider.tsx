import React from "react";
import { View } from "react-native";

const Divider = () => {
  return (
    <View
      style={{
        borderBottomWidth: 1,
        borderBottomColor: "black",
        width: "auto",
      }}
    />
  );
};

export default Divider;
